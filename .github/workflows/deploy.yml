name: Deploy N&D Financial System

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Protect production configuration
        shell: bash
        run: |
          if [ -f app_config/config.php ]; then
            echo "ERROR: app_config/config.php must not be committed."
            exit 1
          fi

      - name: Deploy public website to Hostinger
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: ${{ secrets.HOSTINGER_PORT }}
          source: "public_html/*,public_html/.htaccess"
          # Hostinger serves this domain from the nested public_html directory.
          # Deploy the contents of the repository's public_html folder there,
          # rather than creating public_html/public_html.
          target: "/home/u446223237/domains/nd.sysdevt.online/public_html"
          overwrite: true
          strip_components: 1

      - name: Deploy non-secret application files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USERNAME }}
          password: ${{ secrets.HOSTINGER_PASSWORD }}
          port: ${{ secrets.HOSTINGER_PORT }}
          source: "app_config/config.example.php,app_config/seed.json"
          target: "/home/u446223237/domains/nd.sysdevt.online"
          overwrite: true
